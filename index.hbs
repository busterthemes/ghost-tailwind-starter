{{!< default}}

<div>
    <div class="container mx-auto p-4">

        <div class="flex flex-col items-center">
        {{#foreach posts}}

            {{> "post"}} {{!-- partials/post.hbs --}}

        {{/foreach}}
        </div>

    </div>
</div>
